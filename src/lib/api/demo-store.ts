import type { Offer, Message, WatchlistItem } from "@/types/database";

/** In-memory demo stores — not persisted to Supabase. */
export const demoOffers: Offer[] = [];
export const demoMessages: Message[] = [];
export const demoWatchlist: WatchlistItem[] = [];
