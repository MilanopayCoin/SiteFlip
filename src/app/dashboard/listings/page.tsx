import { MyListingsClient } from "@/components/dashboard/my-listings-client";

export const metadata = { title: "My Listings" };

export default function MyListingsPage() {
  return <MyListingsClient />;
}
